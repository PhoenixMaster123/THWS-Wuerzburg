package org.example;

import java.io.IOException;
import java.util.LinkedHashMap;
import java.util.List;

public class Main {
    public static void main(String[] args) throws IOException {

        //TODO -> needs to be implements the method for reading and the problems to be executed

        // Wrong interpretation:
        /*LinkedHashMap<String, List<Double>> einkaufen = Tuple.readFileDouble();
        LinkedHashMap<String, List<Integer>> gruppen = Tuple.readFileInt();

        for (String s : einkaufen.keySet())
        {
            System.out.println(s + einkaufen.values());
        }
        for (String s : gruppen.keySet())
        {
            System.out.println(s + gruppen.values());
        }
         */

    }
}